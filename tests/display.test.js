const assert = require("chai").assert;


describe("Display", function() {
    it("true", function() {
        assert.isOk(true);
    });

    it("false", function() {
        assert.isAbove(1, 0);
    });
});
